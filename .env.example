# ─────────────────────────────────────────────────────────────────
# Media Processing Admin System — Environment Variables
# Copy this file to .env and fill in the values for your environment.
# NEVER commit .env to version control.
# ─────────────────────────────────────────────────────────────────

# ── Host / LinuxServer.io container user ─────────────────────────
# Run `id` on the host to get UID/GID matching your media files.
PUID=1000
PGID=1000
TZ=Europe/Moscow

# ── PostgreSQL ────────────────────────────────────────────────────
POSTGRES_USER=app
POSTGRES_PASSWORD=changeme_postgres_password
POSTGRES_DB=mediadb

# ── Redis ─────────────────────────────────────────────────────────
# No password required by default; add requirepass in redis command
# if the deployment needs auth.

# ── Prowlarr ──────────────────────────────────────────────────────
# API key is generated by Prowlarr on first run.
# Obtain from Prowlarr → Settings → General → API Key.
PROWLARR_API_KEY=changeme_prowlarr_api_key

# ── qBittorrent ───────────────────────────────────────────────────
# Web UI port inside the container (exposed only on app_net).
QBITTORRENT_WEBUI_PORT=8080
# Default credentials for linuxserver/qbittorrent; change after first run.
QBITTORRENT_USER=admin
QBITTORRENT_PASSWORD=changeme_qbit_password

# ── Core API ──────────────────────────────────────────────────────
API_PORT=8000
# HS256 JWT secret — generate with: openssl rand -hex 32
JWT_SECRET=changeme_jwt_secret_min_32_chars_long
# Service-to-service key for Player API consumers
PLAYER_API_KEY=changeme_player_api_key
# Public base URL where nginx serves media files (HLS/posters).
# Example: https://media.example.com
# Empty value keeps relative links like /media/converted/<id>/master.m3u8
MEDIA_BASE_URL=
# Secret for nginx secure_link token validation.
# Generate with: openssl rand -hex 32
# Must match `set $hls_signing_secret` in ptrack.ink.conf.
MEDIA_SIGNING_KEY=changeme_media_signing_key
# Signed URL lifetime (Go duration format), e.g. 30s, 2m.
MEDIA_SIGNING_TTL=2m

# Admin credentials for Phase 2 login endpoint.
# Option A (recommended for production): set pre-hashed bcrypt password.
#   Generate with: htpasswd -bnBC 10 '' 'yourpassword' | tr -d ':\n'
# ADMIN_PASSWORD_HASH=$2y$10$...
# Option B (dev convenience): set plaintext — hashed at startup, never stored.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme_admin_password

# ── TMDB (The Movie Database) ─────────────────────────────────────
# API key used in two places:
#   1. API service — TMDBLookup: fetches title + IMDb ID by TMDB ID
#      (used in /upload form to auto-fill movie metadata before upload)
#   2. Worker service — after conversion: downloads TMDB backdrop image
#      as movie poster (replaces ffmpeg-extracted frame when available)
# Obtain from: https://www.themoviedb.org/settings/api
TMDB_API_KEY=changeme_tmdb_api_key

# ── OpenSubtitles ─────────────────────────────────────────────────
# API key from https://www.opensubtitles.com/en/consumers
# Used by the worker to auto-fetch subtitles after conversion and
# by the API for manual re-search from the admin UI.
# If empty, subtitle auto-search is disabled (manual upload still works).
OPENSUBTITLES_API_KEY=changeme_opensubtitles_api_key
# Comma-separated ISO 639-1 language codes for automatic subtitle search.
SUBTITLE_LANGUAGES=en,bn,hi

# ── Worker ────────────────────────────────────────────────────────
WORKER_HEALTH_PORT=8001
# How many torrent downloads can run in parallel (each is a separate qBittorrent task).
# Limited by network bandwidth; 2–4 is reasonable for most setups.
DOWNLOAD_CONCURRENCY=2
# How many ffmpeg conversions can run in parallel.
# ffmpeg is CPU-bound — recommended value: number of CPU cores / 2.
# Setting it too high will cause all jobs to slow down equally.
CONVERT_CONCURRENCY=1

# ── Remote storage transfer (optional — leave RCLONE_REMOTE empty to keep files local) ──
RCLONE_CONFIG_MYREMOTE_TYPE=sftp
RCLONE_CONFIG_MYREMOTE_HOST=45.134.174.84
RCLONE_CONFIG_MYREMOTE_USER=mediarw
RCLONE_CONFIG_MYREMOTE_KEY_FILE=/secrets/mediarw_rclone
RCLONE_REMOTE=myremote
RCLONE_REMOTE_PATH=/storage
TRANSFER_CONCURRENCY=1

# ── Frontend ──────────────────────────────────────────────────────
# Host port that maps to the Next.js container port 3000.
FRONTEND_PORT=3000
# URL of the player app shown in the iframe on the /movies page.
# Runtime variable — no frontend rebuild needed when changed.
# Local dev example: http://127.0.0.1:3100
# Production example: https://player.example.com
PLAYER_URL=
# Server-side URL of the API used by Next.js rewrites to proxy /api/* requests.
# NOT exposed to the browser; client code uses relative /api/... paths.
# Set directly in docker-compose.yml (http://api:8000). Override here only if needed.
# API_URL=http://api:8000
# Optional public upload origin for very large files (bypass CDN/proxy limits).
# Example: https://upload.ptrack.ink
# If empty and current host starts with "admin.", frontend auto-uses
# https://upload.<same-domain> for /api/admin/jobs/upload.
# NEXT_PUBLIC_UPLOAD_ORIGIN=https://upload.ptrack.ink

# ── Application image tags ────────────────────────────────────────
# Tag used for api / worker / frontend images.
# Override individual images with API_IMAGE / WORKER_IMAGE / FRONTEND_IMAGE
# if they live in separate registries.
APP_IMAGE_TAG=latest
# API_IMAGE=app/api
# WORKER_IMAGE=app/worker
# FRONTEND_IMAGE=app/frontend

# ── nginx (optional) ──────────────────────────────────────────────
# Uncomment when nginx service is enabled in docker-compose.yml.
# NGINX_HTTP_PORT=80
