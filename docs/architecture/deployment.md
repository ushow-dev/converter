# Развёртывание и инфраструктура

## Серверы

| Сервер | IP | Роль | SSH |
|---|---|---|---|
| **API** | `178.104.100.36` | Go API + Admin UI + PostgreSQL + Redis | `ssh -i ~/.ssh/id_rsa_personal root@178.104.100.36` |
| **Converter** | `178.104.53.215` | Go Worker + qBittorrent + Prowlarr + Flaresolverr | `ssh -i ~/.ssh/id_ed25519 root@178.104.53.215` |
| **Storage** | `45.134.174.84` | HLS origin (nginx), Player UI. Мигрируется на MinIO. | `ssh -i ~/.ssh/id_rsa_personal root@45.134.174.84` |
| **Scanner** | `213.111.156.183` | Python scanner (incoming files), HTTP API :8080 | `ssh -i ~/.ssh/id_rsa_personal root@213.111.156.183` |
| **MinIO** | `178.63.205.179` | Object storage для сконвертированных фильмов/сериалов. Заменит Storage. | |
| **Edge 1 Singapore** | `67.159.52.120` | Сайт ultrashow.fun + API proxy. Лёгкий трафик. | |
| **Edge 2 Singapore** | TBD | HLS потоки + Player UI. Тяжёлый трафик. | |

---

## Сетевая архитектура: внутренний + внешний слой

Принцип: внутренние серверы (API, Worker, Scanner, MinIO, Storage) **никогда не доступны пользователю напрямую**. Все публичные запросы идут через заменяемые Edge proxy серверы. Если Edge заблокируют — поднимается новый VPS, меняется DNS. Внутренняя инфра не трогается.

### Внутренний слой (закрыт фаерволом, доступ только от Edge proxy)

| Сервер | IP | Открытые порты | Доступ |
|---|---|---|---|
| API | 178.104.100.36 | 8000 (API), 3000 (Frontend) | Только от Edge proxy IP |
| Worker | 178.104.53.215 | — | Закрыт (подключается к API PostgreSQL/Redis) |
| Scanner | 213.111.156.183 | 8080 | Только от Worker IP |
| MinIO | 178.63.205.179 | 9000 | Только от Edge proxy IP |
| Storage (legacy) | 45.134.174.84 | 3100 (Player), 80 (nginx) | Только от Edge proxy IP. Мигрируется на MinIO. |

### Внешний слой (Active-Active, Сингапур)

Два Edge proxy в Сингапуре, разделённые по типу трафика. Разные домены = разный риск блокировки. При блокировке одного — второй продолжает работать.

| Edge сервер | IP | Что проксирует | Трафик |
|---|---|---|---|
| **Edge 1** | 67.159.52.120 | Сайт (ultrashow.fun) + API proxy | Лёгкий (JSON, HTML) |
| **Edge 2** | TBD (новый VPS Singapore) | HLS потоки + Player UI | Тяжёлый (видео .ts сегменты) |

**Почему Active-Active, а не Active-Standby:**
- Блокировка stream-домена (жалоба правообладателя) вероятнее чем блокировка сайта
- Сайт продолжает работать при блокировке стриминга и наоборот
- Нагрузка распределена — HLS не забивает канал сервера с сайтом

### Что нужно закупить

| # | Действие | Стоимость | Приоритет |
|---|---|---|---|
| 1 | VPS Singapore (Edge 2 — HLS + Player) | ~$5/мес | Высокий |
| 2 | Домен для стриминга (напр. `stream-xxx.site`) | ~$3/год | Высокий |
| 3 | Закрыть фаерволом API и MinIO — только от Edge 1 + Edge 2 IP | Бесплатно | Высокий |
| 4 | 3-4 резервных домена (дешёвые зоны .xyz, .site) | ~$10/год | Средний |

### Схема маршрутизации (целевая)

```
ultrashow.fun (Edge 1 — 67.159.52.120, Сингапур)
    ├── /api/*     → API (178.104.100.36:8000)
    └── сайт       — статика + админка фильмов

{stream-домен} (Edge 2 — новый IP, Сингапур)
    ├── /           → Player UI (45.134.174.84:3100 → потом MinIO)
    └── /media/*    → MinIO (178.63.205.179:9000)
                      └── nginx proxy_cache для .ts сегментов (кеш на Edge)

Пользователь знает: ultrashow.fun, {stream-домен}
Пользователь НЕ знает: 178.104.100.36, 178.63.205.179, 45.134.174.84
```

### Аудитория

Основная: Бангладеш, Индия. Сингапур — оптимальное расположение Edge (минимальная задержка до Южной Азии).

### При блокировке

**Заблокировали stream-домен:**
1. Привязать резервный домен к Edge 2 IP (5 мин)
2. Обновить MEDIA_BASE_URL в конфиге плеера
3. Сайт (ultrashow.fun) продолжает работать

**Заблокировали ultrashow.fun:**
1. Привязать резервный домен к Edge 1 IP (5 мин)
2. Стриминг продолжает работать

**Заблокировали IP Edge сервера:**
1. Поднять новый VPS в Сингапуре (~10 мин)
2. Перенаправить домены на новый IP
3. Добавить новый IP в фаервол внутренних серверов

**Внутренняя инфра не трогается ни в одном сценарии.**

---

## Домены и маршрутизация (текущее состояние)

| Домен | Сервер | Назначение | Статус |
|---|---|---|---|
| `admin.pimor.online` | API (178.104.100.36) | Admin UI (Next.js frontend) | Напрямую — **перенести за proxy** |
| `api.pimor.online` | API (178.104.100.36) | Go API (admin + player endpoints) | Напрямую — **перенести за proxy** |
| `pimor.online` | API (178.104.100.36) | Заглушка (404) | Напрямую |
| `media.pimor.online` | Storage (45.134.174.84) | HLS файлы (nginx static) | Напрямую — **перенести за proxy** |
| `player.pimor.online` | Storage (45.134.174.84) | Player UI (Next.js) | Напрямую — **перенести за proxy** |
| `ultrashow.fun` | Edge Singapore (67.159.52.120) | Сайт + админка фильмов | Через proxy ✓ |

Cloudflare: **Full (strict)** SSL. Let's Encrypt сертификаты на каждом сервере.

---

## API Server (178.104.100.36)

### Стек
```
/opt/converter/
├── docker-compose.api.yml   ← активный compose файл
├── .env                     ← секреты (не в git)
├── secrets/                 ← ключи
└── media/                   ← временное хранение субтитров
```

### Сервисы
| Контейнер | Порт (host) | Назначение |
|---|---|---|
| `converter-api-1` | 8000 | Go API |
| `converter-frontend-1` | 3000 | Admin UI |
| `converter-postgres-1` | 5432 | PostgreSQL (доступен Worker-серверу) |
| `converter-redis-1` | 6379 | Redis (доступен Worker-серверу) |

Postgres и Redis открыты на `0.0.0.0`, доступ ограничен ufw только для IP Worker-сервера (178.104.53.215).

### Nginx
Конфиг: `infra/nginx/api-server.conf` → `/etc/nginx/sites-available/pimor.conf`

### Запуск / обновление
```bash
cd /opt/converter
git pull origin main
docker compose -f docker-compose.api.yml build api frontend
docker compose -f docker-compose.api.yml up -d
```

> ⚠️ **ВАЖНО: всегда указывай `-f docker-compose.api.yml`.**
> Если запустить `docker compose` без флага `-f`, Docker использует дефолтный `docker-compose.yml`,
> в котором Redis **не публикует порт 6379**. Воркер потеряет доступ к Redis и перестанет
> обрабатывать задания. Восстановление — перезапустить воркер на Converter Server.

---

## Converter Server (178.104.53.215)

### Стек
```
/opt/converter/
├── docker-compose.worker.yml  ← compose файл (worker без postgres/redis)
├── .env                       ← секреты (не в git)
└── secrets/
    ├── mediarw_rclone         ← SSH ключ для SFTP на Storage
    └── scanner_rclone         ← SSH ключ для SFTP на Scanner
```

### Сервисы
| Контейнер | Порт (host) | Назначение |
|---|---|---|
| `converter-worker-1` | 8001 | Go Worker (health) |
| `converter-qbittorrent-1` | 8080 | qBittorrent WebUI |
| `converter-prowlarr-1` | 9696 | Prowlarr |
| `converter-flaresolverr-1` | — | FlareSolverr (внутренний) |

Worker подключается к PostgreSQL и Redis на API-сервере:
```
DATABASE_URL=postgres://app:...@178.104.100.36:5432/mediadb
REDIS_URL=redis://178.104.100.36:6379
```

### Nginx
Nginx не установлен. Сервисы доступны напрямую по IP.

---

## Storage Server (45.134.174.84)

### Стек
```
/storage/             ← HLS файлы (rclone заливает сюда)
/opt/player/          ← Player UI (Next.js, порт 3100)
```

### Сервисы
| Контейнер | Порт (host) | Назначение |
|---|---|---|
| `ptrack-player` | 127.0.0.1:3100 | Player UI |

### Nginx
Конфиг: `infra/nginx/storage-server.conf` → `/etc/nginx/sites-available/pimor.conf`

---

## Scanner Server (213.111.156.183)

### Стек
```
/opt/converter/scanner/
├── docker-compose.yml
├── scanner/
└── tests/
```

### Сервисы
| Контейнер | Порт (host) | Назначение |
|---|---|---|
| `scanner-scanner-1` | 8080 | Python FastAPI (HTTP API) |
| `scanner-postgres-1` | — | PostgreSQL (внутренний) |

### Nginx
Nginx не установлен. API доступен напрямую: `http://213.111.156.183:8080`.

---

## Поток данных

```
Browser
  │
  ├── admin.pimor.online (Admin UI)
  │     └── api.pimor.online (Go API)
  │           ├── PostgreSQL (API Server)
  │           └── Redis queues (API Server)
  │                 └── Worker (Converter Server)
  │                       ├── qBittorrent / rclone HTTP download
  │                       ├── FFmpeg → HLS
  │                       └── rclone SFTP → /storage (Storage Server)
  │
  └── player.pimor.online (Player UI)
        └── api.pimor.online (player endpoints)
              └── media.pimor.online (HLS segments, nginx static)
```

---

## Конфигурация (env)

Шаблоны:
- **API Server**: `.env.api.example`
- **Worker Server**: `.env.worker.example`
- **Player**: `player/.env.example`
- **Scanner**: `scanner/.env.example`

---

## Первоначальная настройка сервера

### API Server
```bash
git clone https://github.com/ushow-dev/converter.git /opt/converter
cd /opt/converter
cp .env.api.example .env   # заполнить значения
docker compose -f docker-compose.api.yml up -d
```

### Worker Server
```bash
git clone https://github.com/ushow-dev/converter.git /opt/converter
cd /opt/converter
cp .env.worker.example .env   # заполнить значения
mkdir -p secrets
# скопировать SSH ключи в secrets/mediarw_rclone и secrets/scanner_rclone
docker compose -f docker-compose.worker.yml up -d
```

---

## Резервное копирование

| Данные | Где | Метод |
|---|---|---|
| Метаданные фильмов | API Server — Docker volume `postgres_data` | `pg_dump` |
| HLS файлы | Storage Server — `/storage` | rsync / snapshot |
| Конфигурация | `.env` файлы на каждом сервере | ручное резервирование |


Текущая проблема

  Сейчас внутренние серверы (API, Storage) торчат наружу напрямую через домены api.pimor.online, media.pimor.online. Если их заблокируют — блокируется всё. Если пользователь узнает IP — он видит внутреннюю инфру.

  Архитектура: внутренний слой + внешний слой

  Пользователь → Edge Proxy (заменяемый) → Внутренний сервер (постоянный)

  Внутренние серверы никогда не имеют публичных доменов. Edge proxy можно менять за минуты — поднял новый VPS, перенаправил DNS.

  Что нужно проксировать

  ┌────────────┬────────────────────────────┬───────────────────────────────┐
  │   Сервис   │         Что делает         │      Требования к прокси      │
  ├────────────┼────────────────────────────┼───────────────────────────────┤
  │ API        │ JSON запросы (легковесные) │ Низкая задержка, малый трафик │
  ├────────────┼────────────────────────────┼───────────────────────────────┤
  │ Player UI  │ Next.js страница (SSR)     │ Низкая задержка               │
  ├────────────┼────────────────────────────┼───────────────────────────────┤
  │ HLS потоки │ Видеосегменты (тяжёлые)    │ Высокий трафик, кеширование   │
  └────────────┴────────────────────────────┴───────────────────────────────┘

  Предлагаемая схема

  Внутренний слой (не менять, не показывать наружу)

  ┌──────────────────┬─────────────────┬────────────────────────────────┬──────────────────────────────────────┐
  │      Сервер      │       IP        │              Роль              │           Публичный доступ           │
  ├──────────────────┼─────────────────┼────────────────────────────────┼──────────────────────────────────────┤
  │ API              │ 178.104.100.36  │ Go API + Admin UI + DB + Redis │ Закрыть фаерволом — только от прокси │
  ├──────────────────┼─────────────────┼────────────────────────────────┼──────────────────────────────────────┤
  │ Worker           │ 178.104.53.215  │ Конвертер + торренты           │ Закрыт (уже)                         │
  ├──────────────────┼─────────────────┼────────────────────────────────┼──────────────────────────────────────┤
  │ Scanner          │ 213.111.156.183 │ Сканер исходников              │ Закрыт (уже)                         │
  ├──────────────────┼─────────────────┼────────────────────────────────┼──────────────────────────────────────┤
  │ MinIO            │ 178.63.205.179  │ Хранилище HLS                  │ Закрыть — только от прокси           │
  ├──────────────────┼─────────────────┼────────────────────────────────┼──────────────────────────────────────┤
  │ Storage (legacy) │ 45.134.174.84   │ Player UI + HLS (мигрируется)  │ Закрыть после миграции               │
  └──────────────────┴─────────────────┴────────────────────────────────┴──────────────────────────────────────┘

  Внешний слой (заменяемый, смотрит наружу)

  ┌───────────────────────────┬──────────────────────────────────────────────┬──────────────────────────────────────────────┐
  │          Прокси           │                Что проксирует                │               Зачем отдельный                │
  ├───────────────────────────┼──────────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ Edge 1 — API proxy        │ API запросы → 178.104.100.36:8000            │ Легковесный, можно поставить на дешёвый VPS  │
  ├───────────────────────────┼──────────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ Edge 2 — Player proxy     │ Player UI → 45.134.174.84:3100 (потом MinIO) │ SSR, нужен рядом с пользователем             │
  ├───────────────────────────┼──────────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ Edge 3 — Media CDN        │ HLS сегменты → MinIO                         │ Тяжёлый трафик, кеширование, несколько точек │
  ├───────────────────────────┼──────────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ Edge Singapore (уже есть) │ 67.159.52.120 — ultrashow.fun                │ Уже работает                                 │
  └───────────────────────────┴──────────────────────────────────────────────┴──────────────────────────────────────────────┘

  Домены

  ┌───────────────┬───────────┬───────────────────────┬─────────────────────────────────┐
  │     Домен     │    Тип    │      Привязан к       │           Назначение            │
  ├───────────────┼───────────┼───────────────────────┼─────────────────────────────────┤
  │ pimor.online  │ Текущий   │ API сервер напрямую   │ Проблема — светит реальный IP   │
  ├───────────────┼───────────┼───────────────────────┼─────────────────────────────────┤
  │ ultrashow.fun │ Текущий   │ Edge Singapore        │ ОК — через прокси               │
  ├───────────────┼───────────┼───────────────────────┼─────────────────────────────────┤
  │ Купить:       │           │                       │                                 │
  ├───────────────┼───────────┼───────────────────────┼─────────────────────────────────┤
  │ домен1.com    │ Новый     │ Edge 1 (API proxy)    │ API endpoint для плеера и сайта │
  ├───────────────┼───────────┼───────────────────────┼─────────────────────────────────┤
  │ домен2.com    │ Новый     │ Edge 3 (Media CDN)    │ HLS потоки для плеера           │
  ├───────────────┼───────────┼───────────────────────┼─────────────────────────────────┤
  │ домен3.com    │ Резервный │ Edge 2 (Player proxy) │ Player embed URL                │
  └───────────────┴───────────┴───────────────────────┴─────────────────────────────────┘

  Минимальная закупка

  ┌────────────────────┬────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────┐
  │        Что         │   Кол-во   │                                              Зачем                                              │
  ├────────────────────┼────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ VPS для Edge/Proxy │ 1-2 шт     │ Nginx reverse proxy. Дешёвые (1-2 ядра, 1GB RAM). Один может совмещать API proxy + Player proxy │
  ├────────────────────┼────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Домены             │ 2-3 шт     │ Один для API, один для медиа. Третий — резерв. Дешёвые зоны (.site, .xyz, .online)              │
  ├────────────────────┼────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Cloudflare         │ Бесплатный │ DNS + DDoS protection + скрытие IP прокси                                                       │
  └────────────────────┴────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────┘

  Итоговая таблица — что нужно

  ┌─────┬─────────────────────────────────────────────────────┬───────────┬───────────┐
  ├─────┼─────────────────────────────────────────────────────┼───────────┼───────────┤
  │ 1   │ VPS Europe (API + Player proxy)                     │ ~$5/мес   │ Высокий   │
  ├─────┼─────────────────────────────────────────────────────┼───────────┼───────────┤
  │ 2   │ Домен для API (напр. newapi.xyz)                    │ ~$3/год   │ Высокий   │
  ├─────┼─────────────────────────────────────────────────────┼───────────┼───────────┤
  │ 3   │ Домен для Media CDN (напр. cdn-stream.site)         │ ~$3/год   │ Высокий   │
  ├─────┼─────────────────────────────────────────────────────┼───────────┼───────────┤
  │ 4   │ Закрыть фаерволом API и MinIO — только от прокси IP │ Бесплатно │ Высокий   │
  ├─────┼─────────────────────────────────────────────────────┼───────────┼───────────┤
  │ 5   │ VPS Asia (второй Media CDN edge, ближе к аудитории) │ ~$5/мес   │ Средний   │
  ├─────┼─────────────────────────────────────────────────────┼───────────┼───────────┤
  │ 6   │ Резервный домен                                     │ ~$3/год   │ Низкий    │
  └─────┴─────────────────────────────────────────────────────┴───────────┴───────────┘

  Схема после

  ultrashow.fun (Edge Singapore)
      ├── /api/*     → Edge Europe → API (178.104.100.36)
      ├── /player/*  → Edge Europe → Player UI (Storage/MinIO)
      └── /stream/*  → Edge Europe → MinIO (178.63.205.179)

  newapi.xyz (Edge Europe) ← плеер embed вызывает сюда
      └── → API (178.104.100.36)

  cdn-stream.site (Edge Europe / Edge Asia)
      └── → MinIO (178.63.205.179)

  Пользователь знает: ultrashow.fun, newapi.xyz, cdn-stream.site
  Пользователь НЕ знает: 178.104.100.36, 178.63.205.179, 45.134.174.84

  Если newapi.xyz блокируют → покупаешь newapi2.xyz, привязываешь к тому же Edge, меняешь конфиг плеера. Внутренняя инфра не трогается.
